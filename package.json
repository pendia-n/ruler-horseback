{
  "name": "rulerhorseback",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "PATH=$HOME/.cargo/bin:$PATH tauri dev",
    "build": "PATH=$HOME/.cargo/bin:$PATH tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
